# All The Things

**It's a suggest mode for both pages *and* entries.**

## Installation

Copy the `AllTheThings` folder into `site/addons/`, and then use as a mode in your suggest field!

## Usage

**Example Fieldset**
```yaml
fields:
  related_links:
    type: suggest
    display: Related Links
    mode: AllTheThings
```